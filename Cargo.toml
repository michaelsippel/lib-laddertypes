[package]
authors = ["Michael Sippel <micha@fragmental.art>"]
edition = "2018"
name = "laddertypes"
version = "0.1.0"

[dependencies]
tiny-diagnostics = { path = "../lib-tinydiagnostics" }
tiny-ansi = { version = "0.1.0", optional = true }

[dev-dependencies]
pretty_assertions = "*"

[features]
pretty = ["dep:tiny-ansi"]
